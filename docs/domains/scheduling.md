# Scheduling

The Scheduling domain provides calendar-oriented platform scheduling.

## What It Provides

- Calendar and calendar-event domain services.
- API exposure for scheduling features through the aggregate host.
- Integration points used by workflow and hosted-services processing.

## Why It Matters

Consumers get shared scheduling primitives that other platform features can depend on instead of each application creating its own calendar model.

